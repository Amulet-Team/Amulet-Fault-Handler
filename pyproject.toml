[build-system]
requires = [
    "setuptools>=42",
    "versioneer",
    "pybind11~=3.0",
    "wheel"
]
build-backend = "setuptools.build_meta"

[project]
name = "amulet-faulthandler"
authors = [
    {name = "James Clare"},
]
description = "A library to report tracebacks from C/C++ faults."
dynamic = ["version", "readme"]
requires-python = ">=3.11"
classifiers = [
    "Programming Language :: Python :: 3",
    "Operating System :: OS Independent",
]

[project.optional-dependencies]
dev = [
    "setuptools>=42",
    "types-setuptools",
    "versioneer",
    "types-versioneer",
    "wheel",
    "pybind11_stubgen>=2.5.4",
    "black>=22.3",
    "isort",
    "autoflake",
    "mypy",
    "types-pyinstaller",
]

[project.urls]
Homepage = "https://www.amuletmc.com"
Repository = "https://github.com/Amulet-Team/Amulet-Fault-Handler"
Issues = "https://github.com/Amulet-Team/Amulet-Fault-Handler/issues"

[tool.setuptools]
include-package-data = false

[tool.setuptools.package-data]
"*" = [
    "*Config.cmake",
    "**/*.hpp",
    "**/*.dll",
    "**/*.so",
    "**/*.dylib",
    "**/*.lib",
]

[tool.setuptools.dynamic]
readme = {file = ["README.md"], content-type = "text/markdown"}

[project.entry-points.pyinstaller40]
hook-dirs = "amulet_faulthandler.__pyinstaller:get_hook_dirs"

[tool.versioneer]
VCS = "git"
style = "pep440"
versionfile_source = "src/amulet_faulthandler/_version.py"
versionfile_build = "amulet_faulthandler/_version.py"
tag_prefix = ""
parentdir_prefix = "amulet_faulthandler-"
